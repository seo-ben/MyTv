import '../../utils/basic_screen_imports.dart';

class WatchListItemWidget extends StatelessWidget {
  const WatchListItemWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
